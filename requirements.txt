# Smart Intersection Signal Control — 의존성
# Python 3.10+ 권장
# SUMO 1.20+ 별도 설치 필요 (https://eclipse.dev/sumo/)

# ── 강화학습 + SUMO ──
torch>=2.0
sumo-rl>=1.4.5
gymnasium>=0.29

# ── 시각화 / 데이터 ──
numpy>=1.24
matplotlib>=3.7

# ── (보류) perception/CCTV 줄기 — archive/perception_track/ 에서만 사용 ──
# 1학기 SUMO 전용 작업엔 불필요. 보관 코드 실행 시에만 설치.
# ultralytics>=8.0      # YOLO 차량 탐지
# opencv-python>=4.8    # 영상 처리
# requests>=2.31        # ITS OpenAPI 호출
