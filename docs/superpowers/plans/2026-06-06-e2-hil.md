# E2 검지기 관측 + 라즈베리파이 HIL 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 카메라 대신 SUMO E2(laneAreaDetector) 검지기로 관측을 구성해 신호제어 정책을 재학습하고, 그 정책을 라즈베리파이에서 numpy로 실시간 추론(HIL)한다.

**Architecture:** (1) `network/e2.add.xml` 검지기 정의 → (2) `e2_observation.py` 커스텀 관측함수가 검지기값을 state로 변환 → (3) `smart_signal.py --obs e2` 로 재학습 → (4) `evaluate.py` 4자 비교 → (5) `export_weights.py`로 .npz 추출 → `edge_server.py`가 Pi에서 numpy 추론 → `demo.py --hil`이 SUMO와 소켓 연결.

**Tech Stack:** Python, sumo_rl, PyTorch(학습/내보내기), numpy(엣지 추론), TCP 소켓(JSON line protocol), sumolib.

**기준 사실 (확인됨):** TLS id=`center`, 진입 차로 12개(`E2C_0..2`,`N2C_0..2`,`S2C_0..2`,`W2C_0..2`), 각 길이 186.4m. 검지기 ID 규약 = `"e2_" + 차로ID`. E2 관측 차원 = phase one-hot(4) + min_green(1) + 4피처×12차로(48) = **53D**.

**세션 추가 사실 (2026-06-06):** ① E2 커버 길이 = **150m**(차로당 cap 20대, 포화 시 클리핑 회피). ② 신호체계 정비 — 한국 표준 반영해 **비보호 좌회전 제거(좌회전은 보호현시에서만), 우회전 상시 비보호(`g`)**. 녹색현시 4개·`Discrete(4)`·53D 불변. (Task 1.0 참조)

---

## Phase 1 — E2 검지기 + 관측함수

### Task 1.0: 신호체계 정비 (한국 표준화) — 세션 추가, 완료
**Files:** Modify `network/intersection.net.xml` (tlLogic `center` 10현시 state)

배경: GUI 검토 중 비보호 좌회전이 비현실적으로 보여 한국 대형 교차로 표준에 맞춰 신호 재정의.

- [x] **Step 1: 좌회전 비보호 제거 + 우회전 상시** — tlLogic `center` 의 녹색/황색 현시 state 교체. 좌회전(lane2)은 보호현시에서만 `G`, 그 외 `r`. 우회전(lane0)은 녹색·황색 동안 상시 `g`(양보), 2초 전적색 구간만 `r`. 직진(lane1) 불변. 녹색현시 4개 유지(전적색·황색은 sumo_rl 재생성, 'y'/all-r 이라 녹색 미카운트).
  - 변경 후 녹색현시: `gGrgrrgGrgrr`(남북직진) / `grGgrrgrGgrr`(남북좌) / `grrgGrgrrgGr`(동서직진) / `grrgrGgrrgrG`(동서좌).
- [x] **Step 2: 검증** — `sumo_rl.SumoEnvironment` reset 후 `num_green_phases == 4`, `action_space == Discrete(4)`. **통과**(53D·RL 구조 불변). 경고 `Missing yellow phase`(우회전 g→전적색)는 무해.

> 영향: Phase 2 재학습·Phase 3 평가·Phase 4 데모 모두 이 새 신호 기준. 학습 전이라 비용 0.

---

### Task 1.1: 검지기 생성 스크립트
**Files:** Create `make_e2_detectors.py`, 산출 `network/e2.add.xml`

- [x] **Step 1: 스크립트 작성**
```python
"""network/e2.add.xml 생성 — TLS 진입 차로마다 E2(laneAreaDetector) 1개.
정지선(차로 끝) 앞 최대 150m 커버. ID 규약 'e2_'+차로ID (e2_observation 과 동일).
차로가 150m 미만이면 전체 커버. 새 네트워크에도 그대로 동작(차로 자동 탐색)."""
import os, sys
os.environ.setdefault("SUMO_HOME", r"C:/Program Files (x86)/Eclipse/Sumo")
sys.path.append(os.path.join(os.environ["SUMO_HOME"], "tools"))
import sumolib

NET, OUT, COVER = "network/intersection.net.xml", "network/e2.add.xml", 150.0
net = sumolib.net.readNet(NET)
lanes = set()
for tls in net.getTrafficLights():
    for conn in tls.getConnections():
        lanes.add(conn[0].getID())

rows = ['<additional>']
for lane in sorted(lanes):
    L = net.getLane(lane).getLength()
    length = min(COVER, L - 0.1)
    pos = max(0.0, L - length)
    rows.append(f'    <laneAreaDetector id="e2_{lane}" lane="{lane}" '
                f'pos="{pos:.2f}" length="{length:.2f}" freq="100000" '
                f'file="e2_detectors_output.xml"/>')
rows.append('</additional>')
with open(OUT, "w") as f:
    f.write("\n".join(rows) + "\n")
print(f"[OK] {OUT} - {len(lanes)} detectors")  # cp949 콘솔 대응(em-dash 회피)
```

- [x] **Step 2: 실행하여 생성**

Run: `python make_e2_detectors.py`
Expected: `[OK] network/e2.add.xml - 12 detectors` ✓

- [x] **Step 3: 산출물 확인**

Run: `python -c "print(open('network/e2.add.xml').read())"`
Expected: `<laneAreaDetector id="e2_E2C_0" lane="E2C_0" pos="36.40" length="150.00" .../>` 등 12줄. ✓

- [x] **Step 4: 출력파일 gitignore + 커밋**
```bash
printf "\n# E2 검지기 출력(재생성 가능)\ne2_detectors_output.xml\n" >> .gitignore
git add make_e2_detectors.py network/e2.add.xml .gitignore
git commit -m "feat(e2): E2 검지기 생성 스크립트 + e2.add.xml (12 detectors)"
```

---

### Task 1.2: E2 관측함수
**Files:** Create `e2_observation.py`

- [x] **Step 1: 관측함수 작성** — env에서 **53D·0~1 범위 PASS**. ⚠️ 인계메모: sumo_rl 이 `self.lanes` 설정 **전에** observation_class 를 생성하므로 계획서 원안(`__init__`에서 `self.ts.lanes` 접근)은 `AttributeError`. → `det_ids`/`_caps` 를 첫 호출 때 지연 계산하도록 수정(`_ensure_caps`). 결과/차원 동일.
```python
"""E2(laneAreaDetector) 기반 관측함수 — 카메라 대신 현장 검지기로 state 구성.
sumo_rl DefaultObservationFunction 과 동일한 phase one-hot + min_green 머리에,
차로별 [대기열, 밀도, 정지수, occupancy] 4피처를 검지기에서 읽어 붙인다(0~1 정규화).
검지기 ID 규약 'e2_'+차로ID (make_e2_detectors.py 와 동일)."""
import numpy as np
from gymnasium import spaces
from sumo_rl.environment.observations import ObservationFunction
from sumo_rl.environment.traffic_signal import TrafficSignal

VEH_LEN = 7.5  # 차량 길이(5) + 최소 간격(2.5) 근사

class E2ObservationFunction(ObservationFunction):
    def __init__(self, ts: TrafficSignal):
        super().__init__(ts)
        self.det_ids = ["e2_" + lane for lane in self.ts.lanes]
        self._caps = None

    def _ensure_caps(self):
        if self._caps is None:
            self._caps = {d: max(self.ts.sumo.lanearea.getLength(d) / VEH_LEN, 1.0)
                          for d in self.det_ids}

    def __call__(self) -> np.ndarray:
        ts = self.ts
        self._ensure_caps()
        la = ts.sumo.lanearea
        phase_id = [1 if ts.green_phase == i else 0 for i in range(ts.num_green_phases)]
        min_green = [0 if ts.time_since_last_phase_change < ts.min_green + ts.yellow_time else 1]
        queue, density, halting, occ = [], [], [], []
        for d in self.det_ids:
            cap = self._caps[d]
            queue.append(min(1.0, la.getJamLengthVehicle(d) / cap))
            density.append(min(1.0, la.getLastStepVehicleNumber(d) / cap))
            halting.append(min(1.0, la.getLastStepHaltingNumber(d) / cap))
            occ.append(la.getLastStepOccupancy(d) / 100.0)
        return np.array(phase_id + min_green + queue + density + halting + occ,
                        dtype=np.float32)

    def observation_space(self) -> spaces.Box:
        n = self.ts.num_green_phases + 1 + 4 * len(self.ts.lanes)
        return spaces.Box(low=np.zeros(n, dtype=np.float32),
                          high=np.ones(n, dtype=np.float32))
```

- [x] **Step 2: 커밋**
```bash
git add e2_observation.py
git commit -m "feat(e2): E2 기반 커스텀 관측함수 (차로별 대기열/밀도/정지/occupancy)"
```

---

### Task 1.3: 관측 스모크 테스트
**Files:** Create `tests/test_e2_observation.py`

- [ ] **Step 1: 테스트 작성 (차원 53 + 0~1 범위 검증)**
```python
"""E2 관측이 정상 차원(53D)·범위(0~1)로 나오는지 스모크 검증. 직접 실행."""
import os, sys
os.environ.setdefault("SUMO_HOME", r"C:/Program Files (x86)/Eclipse/Sumo")
import numpy as np, sumo_rl
from e2_observation import E2ObservationFunction

def main():
    env = sumo_rl.SumoEnvironment(
        net_file="network/intersection.net.xml",
        route_file="scenarios/eval/medium.rou.xml",
        use_gui=False, num_seconds=300, min_green=15, max_green=60,
        single_agent=True, sumo_warnings=False,
        observation_class=E2ObservationFunction,
        additional_sumo_cmd="-a network/e2.add.xml --no-step-log")
    obs, _ = env.reset()
    for _ in range(20):
        obs, *_ = env.step(env.action_space.sample())
    env.close()
    obs = np.asarray(obs)
    assert obs.shape == (53,), f"기대 53D, 실제 {obs.shape}"
    assert np.all(np.isfinite(obs)), "비유한값 존재"
    assert obs.min() >= 0.0 and obs.max() <= 1.0001, f"범위 위반 [{obs.min()},{obs.max()}]"
    print("[PASS] E2 관측 53D, 0~1 범위 OK")

if __name__ == "__main__":
    main()
```

- [ ] **Step 2: 실행하여 통과 확인**

Run: `python tests/test_e2_observation.py`
Expected: `[PASS] E2 관측 53D, 0~1 범위 OK`
(실패 시: `network/e2.add.xml` 경로/검지기 ID 규약 불일치 점검.)

- [ ] **Step 3: 커밋**
```bash
git add tests/test_e2_observation.py
git commit -m "test(e2): E2 관측 차원/범위 스모크 테스트"
```

---

### Task 1.4: E2 실시간 모니터 — 세션 추가, 완료
**Files:** Create `monitor_e2.py`

목적: E2 기능/성능을 눈으로 검증할 자료. 관측함수와 **동일 getter**(jam/veh/halting/occupancy)로 차로별 값을 주기마다 콘솔 테이블 + CSV(`results/e2_monitor.csv`) 출력. `--gui` 시 SUMO 창 동시 표시(`--delay` 지원), 창 닫기/Ctrl+C 시 그래이스풀 종료 후 저장.

- [x] **Step 1: 스크립트 작성** — TraCI 기반. 인자 `--scenario/--gui/--seconds/--interval/--delay/--csv`.
- [x] **Step 2: 검증** — `python monitor_e2.py --scenario medium --seconds 60 --interval 30` → 12 detectors 테이블 + CSV, exit 0. `--gui --scenario saturated` 로 새 신호 위 대기열 차오름 육안 확인(직진차로 queue 17/cap 20, 클리핑 없음 = 150m 결정 입증).

> 비고: 모니터는 신호를 **제어하지 않음**(고정신호 위에서 관측만 표시). RL 제어 거동은 Phase 2 재학습 후 Phase 3/4 에서.

---

## Phase 2 — E2 재학습

### Task 2.1: smart_signal.py 에 --obs 플래그
**Files:** Modify `smart_signal.py`

- [ ] **Step 1: argparse 에 --obs 추가** (`smart_signal.py:64` `args = parser.parse_args()` 직전)
```python
parser.add_argument("--obs", choices=["global", "e2"], default="global",
                    help="global: sumo_rl 기본(전지적) / e2: E2 검지기 기반")
```

- [ ] **Step 2: import sumo_rl 직후 ENV_KWARGS 구성** (`smart_signal.py:70` 아래)
```python
ENV_KWARGS = {}
MODEL_NAME = "smart_signal.pth"
if args.obs == "e2":
    from e2_observation import E2ObservationFunction
    ENV_KWARGS = dict(observation_class=E2ObservationFunction,
                      additional_sumo_cmd="-a network/e2.add.xml")
    MODEL_NAME = "smart_signal_e2.pth"
print(f"[INFO] 관측 모드: {args.obs} → 모델 파일 {MODEL_NAME}")
```

- [ ] **Step 3: probe_env 에 적용** (`smart_signal.py:108-116` 의 `SumoEnvironment(...)` 호출에서 `single_agent=True,` 다음 줄에 추가)
```python
        single_agent=True,
        **ENV_KWARGS,
```

- [ ] **Step 4: 학습 env 에 적용** (`smart_signal.py:235-243` 의 `SumoEnvironment(...)` 호출에서 `single_agent=True,` 다음 줄에 추가)
```python
        single_agent=True,
        **ENV_KWARGS,
```

- [ ] **Step 5: 체크포인트/최종 파일명에 모드 반영** — 체크포인트(`smart_signal.py:296`)와 최종저장(`smart_signal.py:311`) 두 곳 수정
```python
# 296행 부근:
        ckpt_path = os.path.join(args.out_dir, f"checkpoint_{args.obs}_ep{episode}.pth")
# 311행 부근:
final_path = os.path.join(args.out_dir, MODEL_NAME)
```

- [ ] **Step 6: 체크포인트 gitignore 패턴 확장 + 커밋**
```bash
# .gitignore 의 "results/checkpoint_ep*.pth" 를 아래로 교체(접두 다양화 대응)
python -c "import re,io; p='.gitignore'; s=open(p,encoding='utf-8').read(); s=s.replace('results/checkpoint_ep*.pth','results/checkpoint_*.pth'); open(p,'w',encoding='utf-8').write(s)"
git add smart_signal.py .gitignore
git commit -m "feat(e2): smart_signal --obs {global,e2} (관측소스 전환 + 모델명 분리)"
```

---

### Task 2.2: E2 학습 스모크 (5ep)
- [ ] **Step 1: 짧은 학습으로 루프 검증**

Run: `python smart_signal.py --obs e2 --episodes 5`
Expected: `[INFO] STATE_SIZE = 53 ...` 출력 후 `Episode 1/5 ... ~ Episode 5/5`, 에러 없이 종료. `results/smart_signal_e2.pth` 생성.

- [ ] **Step 2: 산출 확인 후 스모크 모델 제거(전체 학습으로 덮어쓸 것)**

Run: `python -c "import torch; c=torch.load('results/smart_signal_e2.pth',map_location='cpu'); print(c['state_size'], c['action_size'])"`
Expected: `53 2`

---

### Task 2.3: 전체 재학습 (하룻밤)
- [ ] **Step 1: 1000ep 재학습 (백그라운드/야간)**

Run: `python smart_signal.py --obs e2 --episodes 1000`
Expected: ~10시간 후 `학습 완료. 최종 모델: results/smart_signal_e2.pth`. `results/training_log.csv` 에 보상 추이 기록(후반 reward가 god-view 학습 때와 유사 스케일로 수렴하는지 확인).

- [ ] **Step 2: 모델 커밋** (smart_signal_e2.pth 는 최종모델이므로 추적)
```bash
git add results/smart_signal_e2.pth
git commit -m "feat(e2): E2 관측 기반 SmartSignal 1000ep 재학습 모델"
```

---

## Phase 3 — 재평가 4자 비교

### Task 3.1: evaluate.py 모드 확장 (fixed/webster/rl_global/rl_e2)
**Files:** Modify `evaluate.py`

- [ ] **Step 1: 모델 로드부를 2개(global/e2)로 확장** — `evaluate.py:78-101`(policy_net 단일 로드 블록)을 아래로 교체
```python
# ── 모델 로드 (global / e2 각각, 있으면 모드 추가) ──
def _load_dqn(path):
    if not os.path.isfile(path):
        return None, None
    ckpt = torch.load(path, map_location=device)
    weights = ckpt["policy_net"] if isinstance(ckpt, dict) and "policy_net" in ckpt else ckpt
    ss = ckpt.get("state_size", weights["net.0.weight"].shape[1])
    a_s = ckpt.get("action_size", weights[max(k for k in weights if k.endswith(".weight"))].shape[0])
    ng = ckpt.get("num_green_phases", 4)
    net = DQN(ss, a_s).to(device); net.load_state_dict(weights); net.eval()
    return net, ng

NETS = {}
net_g, ng_g = _load_dqn("results/smart_signal.pth")
if net_g is not None:
    NETS["rl_global"] = net_g
net_e, ng_e = _load_dqn("results/smart_signal_e2.pth")
if net_e is not None:
    NETS["rl_e2"] = net_e
NUM_GREEN_PHASES = ng_g or ng_e or 4
MODES = ["fixed", "webster"] + list(NETS.keys())
print(f"[INFO] 평가 모드: {MODES}")
```

- [ ] **Step 2: rl 모드별 관측·검지기 분기** — `run_episode` 의 env 생성(`evaluate.py:120-129`)에서 mode 가 `rl_e2` 면 E2 관측 사용. 함수 본문 맨 앞에 추가:
```python
    extra = "--no-step-log"
    obs_class = None
    if mode == "rl_e2":
        from e2_observation import E2ObservationFunction
        obs_class = E2ObservationFunction
        extra = "-a network/e2.add.xml --no-step-log"
```
그리고 같은 호출의 `additional_sumo_cmd="--no-step-log",` 를 아래로 교체:
```python
        additional_sumo_cmd=extra,
        **({"observation_class": obs_class} if obs_class else {}),
```

- [ ] **Step 3: 정책 선택을 모드별 NETS 로** — `run_episode` 의 rl 분기(`evaluate.py:156-160`, `else:` 블록)를 교체
```python
        else:  # rl_global / rl_e2
            net = NETS[mode]
            with torch.no_grad():
                q = net(torch.FloatTensor(np.array(obs, dtype=np.float32)).to(device))
                dqn_action = int(q.argmax().item())
            action = cur_phase if dqn_action == 0 else (cur_phase + 1) % NUM_GREEN_PHASES
```

- [ ] **Step 4: 출력 파일명/차트 스타일에 신규 모드 반영** — `OUT_CSV`(`evaluate.py:182`)와 차트 STYLE/LABELS 갱신
```python
# 182행:
OUT_CSV = "results/evaluation_e2.csv"
# LABELS(103행 부근) 교체:
LABELS = {"fixed": "Fixed", "webster": "Webster", "rl_global": "SmartSignal(god-view)", "rl_e2": "SmartSignal(E2)"}
# STYLE(268행 부근)에 추가:
STYLE = {'fixed': ('#546E7A','Fixed'), 'webster': ('#8E7CC3','Webster'),
         'rl_global': ('#00897B','SmartSignal(god-view)'), 'rl_e2': ('#FF7043','SmartSignal(E2)')}
# out_png(301행 부근):
out_png = "results/evaluation_e2_by_scenario.png"
```

- [ ] **Step 5: 커밋**
```bash
git add evaluate.py
git commit -m "feat(e2): evaluate 4자 비교(fixed/webster/rl_global/rl_e2)"
```

### Task 3.2: 재평가 실행
- [ ] **Step 1: 평가 실행**

Run: `python evaluate.py --episodes 8`
Expected: `[INFO] 평가 모드: ['fixed', 'webster', 'rl_global', 'rl_e2']`, 5시나리오 진행 후 종합표 출력. `results/evaluation_e2.csv`(160행=5×8×4) + `results/evaluation_e2_by_scenario.png` 생성. **핵심 확인**: rl_e2 가 fixed/webster 능가, rl_global 과 견줄 만함.

- [ ] **Step 2: 결과 커밋**
```bash
git add results/evaluation_e2.csv results/evaluation_e2_by_scenario.png
git commit -m "feat(e2): E2 vs god-view vs baseline 4자 평가 결과/차트"
```

---

## Phase 4 — 라즈베리파이 HIL

### Task 4.1: 가중치 내보내기 (.pth → .npz)
**Files:** Create `export_weights.py`

- [ ] **Step 1: 작성**
```python
"""학습 모델(.pth)을 numpy(.npz)로 내보내 Pi에서 torch 없이 추론하게 한다."""
import argparse, torch, numpy as np
ap = argparse.ArgumentParser()
ap.add_argument("--model", default="results/smart_signal_e2.pth")
ap.add_argument("--out",   default="results/smart_signal_e2.npz")
a = ap.parse_args()
ckpt = torch.load(a.model, map_location="cpu")
w = ckpt["policy_net"] if isinstance(ckpt, dict) and "policy_net" in ckpt else ckpt
np.savez(a.out,
    W0=w["net.0.weight"].numpy(), b0=w["net.0.bias"].numpy(),
    W2=w["net.2.weight"].numpy(), b2=w["net.2.bias"].numpy(),
    W4=w["net.4.weight"].numpy(), b4=w["net.4.bias"].numpy())
print(f"[OK] {a.out}")
```

- [ ] **Step 2: 실행 + 커밋**

Run: `python export_weights.py`
Expected: `[OK] results/smart_signal_e2.npz`
```bash
git add export_weights.py results/smart_signal_e2.npz
git commit -m "feat(hil): .pth -> .npz 가중치 내보내기 + e2 npz"
```

### Task 4.2: 엣지 추론서버 (numpy)
**Files:** Create `edge_server.py`

- [ ] **Step 1: 작성**
```python
"""엣지(라즈베리파이) 추론 서버 — torch 없이 numpy 로 DQN 추론.
줄단위 JSON over TCP: {"obs":[...]} 수신 → {"action":int} 회신. demo.py --hil 와 짝."""
import argparse, json, socket
import numpy as np

def load(path):
    d = np.load(path)
    return (d["W0"], d["b0"]), (d["W2"], d["b2"]), (d["W4"], d["b4"])

def infer(layers, x):
    (W0,b0),(W2,b2),(W4,b4) = layers
    x = np.asarray(x, dtype=np.float32)
    h = np.maximum(0, W0 @ x + b0)
    h = np.maximum(0, W2 @ h + b2)
    return int(np.argmax(W4 @ h + b4))

def main():
    ap = argparse.ArgumentParser()
    ap.add_argument("--weights", default="smart_signal_e2.npz")
    ap.add_argument("--host", default="0.0.0.0")
    ap.add_argument("--port", type=int, default=9999)
    a = ap.parse_args()
    layers = load(a.weights)
    srv = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
    srv.setsockopt(socket.SOL_SOCKET, socket.SO_REUSEADDR, 1)
    srv.bind((a.host, a.port)); srv.listen(1)
    print(f"[edge] listening on {a.host}:{a.port}  (weights={a.weights})")
    while True:
        conn, addr = srv.accept(); print("[edge] connected", addr)
        with conn, conn.makefile("rwb") as f:
            for line in f:
                try:
                    act = infer(layers, json.loads(line.decode())["obs"])
                    f.write((json.dumps({"action": act})+"\n").encode()); f.flush()
                except Exception as e:
                    f.write((json.dumps({"error": str(e)})+"\n").encode()); f.flush()

if __name__ == "__main__":
    main()
```

- [ ] **Step 2: 커밋**
```bash
git add edge_server.py
git commit -m "feat(hil): numpy 엣지 추론 TCP 서버"
```

### Task 4.3: 추론 패리티 테스트 (numpy == torch) — 핵심
**Files:** Create `tests/test_inference_parity.py`

- [ ] **Step 1: 작성**
```python
"""numpy 엣지 추론이 torch 추론과 동일 action 을 내는지 검증(랜덤 100개). 직접 실행."""
import numpy as np, torch
from edge_server import load, infer

NPZ, PTH = "results/smart_signal_e2.npz", "results/smart_signal_e2.pth"

def torch_net():
    import torch.nn as nn
    ck = torch.load(PTH, map_location="cpu")
    w = ck["policy_net"] if "policy_net" in ck else ck
    ss = w["net.0.weight"].shape[1]; a_s = w["net.4.weight"].shape[0]
    net = nn.Sequential(nn.Linear(ss,128),nn.ReLU(),nn.Linear(128,128),nn.ReLU(),nn.Linear(128,a_s))
    sd = {k.replace("net.",""):v for k,v in w.items()}
    net.load_state_dict({f"{i}.{p}":sd[f"{i}.{p}"] for i in (0,2,4) for p in ("weight","bias")})
    net.eval(); return net, ss

def main():
    layers = load(NPZ); net, ss = torch_net()
    rng = np.random.default_rng(0)
    for _ in range(100):
        x = rng.random(ss).astype(np.float32)
        a_np = infer(layers, x)
        with torch.no_grad():
            a_t = int(net(torch.from_numpy(x)).argmax().item())
        assert a_np == a_t, f"불일치 np={a_np} torch={a_t}"
    print("[PASS] numpy == torch (100/100)")

if __name__ == "__main__":
    main()
```

- [ ] **Step 2: 실행**

Run: `python tests/test_inference_parity.py`
Expected: `[PASS] numpy == torch (100/100)`

- [ ] **Step 3: 커밋**
```bash
git add tests/test_inference_parity.py
git commit -m "test(hil): numpy/torch 추론 패리티 100/100"
```

### Task 4.4: demo.py 에 --hil 모드
**Files:** Modify `demo.py`

- [ ] **Step 1: argparse 에 HIL 인자 추가** (demo.py 의 parser 마지막 add_argument 뒤)
```python
parser.add_argument("--hil", action="store_true", help="엣지서버(소켓)로 추론 위임")
parser.add_argument("--host", default="127.0.0.1")
parser.add_argument("--port", type=int, default=9999)
```

- [ ] **Step 2: HIL 클라이언트 헬퍼 추가** (demo.py 의 제어 루프 시작 전, 함수 정의부에 추가)
```python
import socket, json
class EdgeClient:
    def __init__(self, host, port):
        self.sock = socket.create_connection((host, port), timeout=5)
        self.f = self.sock.makefile("rwb")
        print(f"[hil] 엣지서버 연결: {host}:{port}")
    def act(self, obs):
        self.f.write((json.dumps({"obs": list(map(float, obs))})+"\n").encode()); self.f.flush()
        return int(json.loads(self.f.readline().decode())["action"])
    def close(self):
        try: self.f.close(); self.sock.close()
        except Exception: pass
```

- [ ] **Step 3: rl 결정 분기에서 HIL 사용** — demo.py 의 rl action 계산부에서, 로컬 torch 추론 대신 `--hil` 이면 EdgeClient 사용. 제어 루프 진입 전 `edge = EdgeClient(args.host,args.port) if args.hil else None`, rl 분기를:
```python
            if edge is not None:
                dqn_action = edge.act(obs)
            else:
                with torch.no_grad():
                    q = policy_net(torch.FloatTensor(np.array(obs, dtype=np.float32)).to(device))
                    dqn_action = int(q.argmax().item())
            action = cur_phase if dqn_action == 0 else (cur_phase + 1) % NUM_GREEN_PHASES
```
루프 종료 후 `if edge: edge.close()`.

> 주의: `--hil` 데모는 E2 모델 기준이므로 demo 도 E2 관측으로 띄워야 함 — demo.py 의 env 생성에 `observation_class`/`-a network/e2.add.xml` 를 `--hil`(또는 별도 `--obs e2`)일 때 적용. (Task 실행 시 demo.py 의 env 생성부 확인 후 동일 패턴으로 추가.)

- [ ] **Step 4: 커밋**
```bash
git add demo.py
git commit -m "feat(hil): demo --hil (엣지서버 소켓 추론)"
```

### Task 4.5: localhost HIL 왕복 검증
- [ ] **Step 1: 엣지서버 기동(터미널 A)**

Run: `python edge_server.py --weights results/smart_signal_e2.npz`
Expected: `[edge] listening on 0.0.0.0:9999`

- [ ] **Step 2: 데모 실행(터미널 B)**

Run: `python demo.py --hil --host 127.0.0.1 --scenario asymmetric --duration 600`
Expected: `[hil] 엣지서버 연결` 후 GUI에서 신호가 정상 제어됨. 서버 콘솔에 connected 로그.

### Task 4.6: 실물 라즈베리파이 전환
- [ ] **Step 1: Pi에 파일 복사** (numpy만 필요, torch 불필요)

Run(예): `scp results/smart_signal_e2.npz edge_server.py pi@<PI_IP>:~/`

- [ ] **Step 2: Pi에서 서버 기동**

Run(Pi): `python3 edge_server.py --weights smart_signal_e2.npz`
Expected: `[edge] listening on 0.0.0.0:9999`

- [ ] **Step 3: PC에서 Pi IP로 데모** (PC·Pi 동일 LAN)

Run(PC): `python demo.py --hil --host <PI_IP> --scenario asymmetric --duration 600`
Expected: Pi가 추론, SUMO가 실시간 제어. **발표 라이브 데모 영상 촬영.**

---

## Phase 5 — 발표 산출물

### Task 5.1: 비교 차트/표 정리
- [ ] **Step 1:** `results/evaluation_e2_by_scenario.png` 와 4자 종합표를 슬라이드용으로 확인. 필요시 제목/색 조정(Task 3.1 STYLE).
- [ ] **Step 2:** 발표 핵심 수치(예: asymmetric에서 E2 모델의 대기시간/throughput vs Fixed) 표로 정리.

### Task 5.2: README/차터 갱신
- [ ] **Step 1:** `README.md` 에 E2 관측 + HIL 줄기 추가(목표·구조·실행법·결과).
- [ ] **Step 2:** `docs/project_charter.md` 의 마일스톤에 M5(E2+HIL) 반영.
- [ ] **Step 3: 커밋**
```bash
git add README.md docs/project_charter.md
git commit -m "docs: E2 관측 + 라즈베리파이 HIL 반영(README/차터)"
```

---

## Self-Review (계획 검증)

- **스펙 커버리지**: §3 E2 피처(①.5)=Task1.2 / 재학습=Phase2 / 4자 평가=Phase3 / HIL(export·서버·demo·Pi)=Phase4 / 관측일관성(E2모델만 Pi)=Task4.1·4.3 명시 / 발표=Phase5. 누락 없음.
- **타입 일관성**: 검지기 ID `e2_<lane>` (make/관측 동일), 가중치 키 `net.0/2/4.{weight,bias}` (export/edge/parity 동일), state 53D (관측/probe/평가 일관).
- **플레이스홀더**: 없음(모든 코드 단계에 실제 코드 포함). demo.py env 생성부는 파일 구조 확인 후 동일 패턴 적용으로 명시.
- **리스크**: E2 수렴(Phase2 로그 확인), numpy/torch 패리티(Task4.3 게이트), Pi 로지스틱스(4.5 localhost 선검증).
